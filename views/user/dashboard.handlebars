<section>
    <div class="form-container">
        <div></div>
        <div class="form-top">
            <div class="form-header"><p class="text-center font-24 fw-bolder">Welcome {{#upperCase user.firstName}} {{/upperCase}} {{#upperCase user.lastName}} {{/upperCase}} to Vudu family</p></div>
            <div class="form-body">
                <div class="fs-3 text-primary">Your Cart</div>
                <table class="table table-striped table-hover">
                    <thead>
                        <th scope="col">Movie Name</th>
                        <th scope="col">Type</th>
                        <th scope="col">Price</th>
                        <th scope="col">Action</th>
                    </thead>
                    <tbody>
                        {{#each carts}}
                        <tr>
                            <td>{{this.title}}</td>
                            <td>{{this.type}}</td>
                            <td>{{this.price}}</td>
                            <td>
                                <a href="/task/delete/{{this.id}}?method=DELETE" class="btn btn-danger">Remove</a>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>

            <div class="form-body">
                <button class="form-button"><a href="/">Click here to go back to Home Page</a> </button>
            </div>
        </div>
        <div></div>
</section>